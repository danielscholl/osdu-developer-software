apiVersion: kiali.io/v1alpha1
kind: Kiali
metadata:
  name: kiali
  namespace: kiali-operator
spec:
  istio_namespace: istio-system
  auth:
    strategy: anonymous
  deployment:
    ingress:
      enabled: false
    namespace: kiali-operator
    logger:
      log_level: info
  server:
    profiler:
      enabled: false
  external_services:
    grafana:
      url: http://prometheus-operator-grafana.monitor.svc.cluster.local:80
    prometheus:
      url: http://prometheus-operated.monitor.svc.cluster.local:9090
    # tracing:
    #   url: http://jaeger-query:16686